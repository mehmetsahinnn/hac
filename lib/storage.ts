// Legacy module — feature removed. Kept as a stub so old API routes still compile.
// Active app uses lib/types.ts plus browser localStorage.
export const storage = {
  getActions: () => [] as unknown[],
}
